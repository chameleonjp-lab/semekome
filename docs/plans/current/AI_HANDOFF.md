# 実装担当への引き継ぎ / 第5版

## Draft PR：R3選択補給を物理発射まで通す回帰検査（2026-09-21）

PR #28で確定した `playerSupplyAllocation` が、物理側の実物ケース生成からP1の取得・配送・装填・発射へそのまま引き継がれることを `tests/rules/r3-player-supply-flow.test.ts` で固定した。シード915の特殊配分から最初の `split_payload` を選び、`case_spawned`、P1の `pickup` / `deliver`、P1を射手とする `projectile_launched`、その後の `projectile_split` と親ケース消費を一つの固定更新列で確認する。

この単位は、既存物理処理の配分受け渡しを回帰検査したもので、全面配送の実装、P2/P3への味方命令、共通WorldStateと物理状態の同一試合統合ではない。敵の補給順は検査対象へ公開せず、物理側の時計・固定座標・ケース所在の責任分担を維持する。R3全体、通常戦の終局、観戦・結果・ランキング、実機・試遊は残件である。

## Draft PR：R3編成画面の補給4種・配分選択と物理戦への引き継ぎ（2026-09-21）

今回のDraft PRでは、出撃準備画面に補給4行の種類・個数選択を追加する。初期値は標準配分（標準3・重量2・防護2・高速1）とし、各行1〜3個、異なる4種類、合計8個だけを開始時に受け付ける。不正な重複・合計値は画面上で拒否し、共通の `validateSupplyAllocation` と同じ契約で検証する。

検証済みの8個列は物理 `createBattle` の `playerSupplyAllocation` へ渡し、物理側の自車バッグと周期更新へ反映する。戦闘画面には自車だけの次の2個を `physicalBattle.getPlayerSupplyPreview` から表示し、敵側の配分・予定は表示しない。ブラウザ検査では入力拒否、物理状態への引き継ぎ、選択範囲内の2個予告を確認する。

この単位は編成入力から物理操作確認版への設定反映と予告表示までであり、全面配送、ケース効果の実戦運用、R3全体、通常戦の終局、観戦・結果・ランキングを完成扱いにしない。ローカルのNode検査は通過済みだが、Playwright実行ファイルは環境に未配置のためブラウザ検査はGitHub ActionsのQualityで確認する。

## Draft PR：R3物理操作確認版への補給配分反映（2026-09-21）

今回のDraft PRでは、物理操作確認版の `physicalBattle.createBattle` でも `playerSupplyAllocation` を受け付け、共通版と同じ8個・4種類・各1〜3個の入力契約を生成入口で検証する。自陣の物理補給バッグと周期更新は指定配分を引き継ぎ、指定を省略した場合と敵側は標準配分のままにする。

`physicalBattle.getPlayerSupplyPreview` は自陣の現在カーソルから次の2個だけを読み取り、周期末尾では次周期の先頭まで続けて返す。読み取りでバッグ、カーソル、周期を変更せず、敵の予定を返す物理APIは追加しない。物理側の時計・固定座標・床ケースは引き続き `physical-battle.ts` が所有し、共通 `BattleState` と二重に進めない。

この単位は物理シミュレーションの設定境界と予告境界までであり、補給選択画面からこの配分を入力する導線、全面配送、R3全体、通常戦の終局、観戦・結果・ランキングを完成扱いにしない。

## Draft PR：R3補給配分の検証と自車プレビュー（2026-09-20）

今回のDraft PRでは、`createBattle` の `playerSupplyAllocation` から自車の補給配分を受け付ける。配分は8個のケース列として渡し、共通スケジュールの入口で「異なる4種類」「各1〜3個」「合計8個」を検証する。標準配分を省略した場合は従来どおり標準3・重量2・防護2・高速1を使い、敵側は標準配分を内部で保持してプレイヤー設定を共有しない。

`getPlayerSupplyPreview` は自車スケジュールの現在位置から次の2個だけを返し、8個周期の末尾では次周期の先頭へ続けて予告する。プレビューは読み取り専用の配列で、スケジュールのカーソルや補給生成を進めない。敵の予定を返すAPIは追加しない。

この単位は共通ルールの配分設定と予告境界であり、補給選択画面、物理操作確認版への設定反映、全面配送、R3全体、通常戦の終局、観戦・結果・ランキングを完成扱いにしない。

今回のDraft PRでは、前回予約した共通 `ammo_carrier` が対象砲台の部屋へ到達した後、次の共通更新で `enqueue_object` を発行して待ち列へ投入する。運搬手の生存世代、予約済みケース、対象砲台の部屋、設備停止、待ち列容量を再検証し、満杯または停止中ならケースを `reserved-carried` のまま保持する。投入が成立した時点で予約を解放し、受入れ時点の砲台設定を共通待ち列スナップショットへ記録する。共通モデルには物理的な受渡し床枠がないため、砲台室到達を共通側の最終受渡し境界とし、物理操作確認版の床枠・運搬AIとは別経路のままにする。

前回のDraft PRでは、予約した共通 `ammo_carrier` が対象砲台の部屋まで、現在の城の正規リンクを一更新に一部屋ずつ進む。移動先は開いている門だけで構成した安定順の経路から選び、予約・ケース所在・生存世代が一致しない場合は移動しない。砲台室への到達までを扱い、床枠への最終受渡しと待ち列投入は未完了である。物理操作確認版の運搬AIとは別の共通経路であり、同じ試合に重ねて動かさない。

前々回のDraft PRでは、共通 `ammo_carrier` が自分の弾薬庫にある床ケースを取得し、補給口に対応する砲台を優先して空いている受渡し枠を出発前に予約した。停止中の砲台は候補から外し、複数の運搬手は人物ID・ケースID・砲台ID・枠番号の安定順で競合を避けた。砲台までの部屋移動、床枠への最終受渡し、待ち列投入は未完了である。

直前のDraft PRでは、共通 `SupplySchedule` を `BattleState.supply` から読み、共通 `stepBattle` が陣営ごとの4補給口について `spawn_supply` を自動生成した。停止・設備停止・床上限（部屋16個）・由来グループ上限・共通遷移拒否では順番を進めず、空き床を補給口に近い安定順で選び、受け入れられたケースだけID・生成元グループ・次回時刻・カーソルを更新する。物理操作確認版の補給生成は別経路のままにして二重生成を避ける。補給選択、全面配送、R3全体、通常戦の終局、観戦・結果・ランキングは未完了である。

今回のDraft PRでは、R3の物理運搬AIに配送先再選択を追加した。出発前予約の砲台が停止・消失・受渡し枠占有になった場合は予約を解放して、同じ陣営の準備済み砲台へ再予約する。全候補が使えない更新ではケースを運搬手の貨物として保持し、床や停止設備へ積み上げない。既存のキュー満載でも2枠の受渡し床へ置ける契約は維持する。共通補給スケジュールの自動生成、R3全体、通常戦の終局、観戦・結果・ランキングは未完了である。

R2aではPR #4の8武器共通ルール・検証済み操作入口・敵4役割の判断基盤を保持し、PR #5の4種の補給・連続移動・運搬・砲撃の操作確認画面を追加した。PR #6では ../../RULE_ENGINE.md の接続契約に従い、src/simulation/r2b-bridge.ts で固定座標の最初の接触と世代付き広場突破を検証済み入力へ変換した。PR #7では `BattleIntent.bridge` を物理 `stepBattle` の同一更新へ接続し、共通 `stepWorld` の状態とイベントを一度だけ取り込む。PR #8ではダッシュの人物接触を1損傷・36更新無敵・選択中荷物一個の落下・0.6床の押し戻しへ接続し、PR #9〜#10では突進実行器と設備接触・停止復旧、PR #11では内部兵の城内目標・局所防衛・退避、直前のDraft PRでは実広場座標・警備突破・跨城侵入を物理固定更新へ接続した。共通ルールは src/simulation/battle.ts、物理操作確認は src/simulation/physical-battle.ts で、公開APIの後者は physicalBattle 名前空間。物理側が時計・乱数・イベント履歴を所有し、状態・命令・座標単位を不正に混用しない。補給効果・修理・共通ルール側の敵判断、観戦画面を含む通常戦は未完了。R2全体の完了ではない。
対象はchameleonjp-lab/semekome。第5版R1の基礎実装と採用配置確認は実装済み。検査の実施範囲は ../../TEST_REPORT.md、次段階は ../../IMPLEMENTATION_PROGRESS.md を参照する。通常戦の完成挙動、実機検査、外部接続・公開登録は未実施である。まずPRODUCT_REQUIREMENTS.md、MAP_ADOPTION.md、SCREEN_FLOW_AND_BATTLEFIELD.md、RESULTS_SCORE_RANKING.md、INITIAL_RULES.json、REQUIREMENTS.json、ENEMY_ROSTER.json、INTERIOR_LAYOUTS.json、ACCEPTANCE_TESTS.jsonを全文読む。核と門の補足はSEVEN_PART_CORE_RULES.md、人物はNPC_ROOMS_AND_RESPAWN.mdを読む。
自陣左で正面右、敵陣右で正面左、中央広場。自陣を90度右回転した採用配置とその左右反転を使う。文字・操作・世界状態を反転共有しない。図にない核と7門を消さず、復活室と分ける。
外装7部位→7門→有効な核一撃、敵30人20秒、広場の敵を倒して前進、相互侵入、主人公5秒観戦復活、名前とカウントダウン、結果のスコア詳細・共有・上位10位・実験場リンクを省かない。外装は城同士の攻撃だけで壊す。
両城の採用図に合わせた4砲台・4弾薬庫・4補給口、敵4/4/8/14、自陣1+2、広場3人・侵入2人、得点式は補足案。自陣4砲台は主人公と補助員2体が運用し、敵の専任射手E01〜E04は敵陣だけで使う。総数を減らして負荷に対応しない。寸法・当たり判定・難易度は未試遊。
ルール番号はsemekome-prototype-0.4-facing-castles。旧第4版と異なる配置と役割を暗黙変換しない。役割・所属・人物番号・生存世代・物体の所在を記録し、画面切替で新しい試合を生成しない。
ランキングは初期公開に必須。ただし接続値は未確定。共有リポジトリの11_ranking_integration_standard.mdを再確認し、同じ開始・結果の再送を一件にする。game_slugや接続処理を名称から推測しない。今回Supabase・公開登録を変更しない。
validate_documents.pyは設計資料だけを検査する。ゲーム検査、実機、試遊、接続確認を合格扱いにしない。mainへ直接push・無断マージ・自動マージ・保護変更・公開はしない。作業ブランチとDraft PRで提出する。
