# 検査記録

## Draft PR：R3共通運搬手の出発前予約（2026-09-19）

対象は、共通WorldStateで生成された床ケースを敵側の共通運搬手が取得し、対応砲台の受渡し枠を予約する変更。人物ID・ケースID・砲台ID・枠番号の安定順、停止中砲台の除外、同じケースと受渡し枠の二重利用防止、予約後も運搬手の所在が一つに保たれることを確認した。砲台までの部屋移動、最終受渡し、待ち列投入、物理操作確認版の運搬AIとの統合は別段階の残件である。

|検査|結果|
|---|---|
|R3共通運搬予約|3件通過。同室ケースの取得と優先砲台枠の予約、2人の運搬手による枠分離、停止砲台から別砲台への再選択を確認|
|既存ルール回帰|全199件通過（`npm test`）|
|型検査|`npm run typecheck` 通過|
|資料検査|`npm run check:docs` 通過（57/57）|
|製品ビルド|`npm run build` 通過|
|ブラウザ|ローカルは未実施。Playwright の Chromium / WebKit 実行ファイルが未配置で起動に失敗し、取得もネットワーク制限でタイムアウトした。GitHub Actions の Chromium / WebKit 18件は成功|
|実機・試遊|未実施。運搬表示、実機、通常戦の試遊は別段階の残件|

共通運搬手は予約済みケースを保持したまま、次の移動・受渡し単位へ引き継ぐ。今回のDraft PRを共通物流全体、R3全体、R2全体、通常戦完成とは扱わない。

## Draft PR：R3共通WorldStateの補給床自動生成（2026-09-19）

対象は、直前に共通化した陣営ごとの8個周期を `BattleState` の4補給口から読み、共通 `stepBattle` が authored floor へケースを自動生成する変更。停止・設備停止・部屋の床上限・未消費グループ上限・床セル競合時の据え置き、受け入れ成功時だけのカーソル進行、ケースIDと生成元グループの一意性を確認した。物理操作確認版の補給生成とは別経路を維持している。補給選択画面、全面的な配送AI、R3全体、通常戦終局、観戦・結果・ランキング、実機・試遊の完成検査ではない。

|検査|結果|
|---|---|
|R3共通WorldState補給生成|3件通過。4補給口の自動生成、停止中の順番保持と再開後の一回生成、部屋16個上限後の再開を確認|
|既存ルール回帰|全196件通過（`npm test`）|
|型検査|`npm run typecheck` 通過|
|資料検査|`npm run check:docs` 通過（57/57）|
|製品ビルド|`npm run build` 通過|
|ブラウザ|ローカルは未実施。Playwright の Chromium / WebKit 実行ファイルが未配置で起動に失敗し、取得もネットワーク制限でタイムアウトした。GitHub Actions の Chromium / WebKit 18件は成功|
|実機・試遊|未実施。補給表示、実機、通常戦の試遊は別段階の残件|

共通側は補給口ID順に空き床を予約し、同じ更新の複数生成でもケース位置・ケースID・生成元グループが衝突しないようにする。共通遷移が受け入れたケースだけスケジュールと補給口の次回時刻を進め、床上限や停止で過去分を一括生成しない。このDraft PRをR3全体、R2全体、通常戦完成とは扱わない。

## Draft PR：R3共通補給スケジュール生成器（2026-09-18）

対象は、車両ごとに4補給口が共有する標準配分の8個周期を共通 `src/logistics` で決定的に生成し、物理補給が共通 `spawn_supply` の成功時だけ順番を消費する変更。補給停止中の据え置き、再開後の一個ずつの生成、同じシード・陣営の再現、配分不正の拒否を確認した。共通 `WorldState` の床自動生成、補給選択画面、全面的な配送AI、実広場・通常戦終局・観戦・結果・ランキング、実機・試遊の完成検査ではない。

|検査|結果|
|---|---|
|R3共通補給スケジュール|3件通過。3周期の配分、入力再現、異常配分拒否、停止中の順番保持と再開後の4補給口一回生成を確認|
|既存ルール回帰|全193件通過（`npm test`）|
|型検査|`npm run typecheck` 通過|
|資料検査|`npm run check:docs` 通過|
|製品ビルド|`npm run build` 通過|
|ブラウザ・実機・試遊|未実施。補給表示、実機、通常戦の試遊は別段階の残件|

`SupplySchedule` は標準8個の配分をシード・陣営・周期でシャッフルし、同じ車両の4補給口は一つの順番を補給口ID順に読む。物理側はケース床、停止理由、上限、共通遷移を確認してから順番を進めるため、失敗した生成で配分・ケースID・グループを消費しない。補給停止中に過去の時刻を埋め合わせず、再開更新では各補給口一個に留める。このDraft PRをR3全体、R2全体、通常戦完成とは扱わない。

## Draft PR：R3配送先の再選択（2026-09-18）

対象はR3の物理運搬AIが、停止中・受渡し枠が空いていない配送先へ貨物を積み上げず、同じ陣営の受け入れ可能な砲台へ配送先を再選択する変更。出発前予約の対象が停止した場合は予約を解放して再予約し、全砲台が停止中ならケースを運搬手の貨物として保持する。キュー満載でも既存の2枠の物理受渡しは維持し、受渡し枠が空いている場合だけ配送候補にする。共通補給スケジュールの自動生成、実広場・跨城侵入、通常戦の終局、観戦・結果・ランキング、実機・試遊の完成検査ではない。

|検査|結果|
|---|---|
|R3配送先再選択|2件通過。停止した予約先から別砲台への再予約、全砲台停止時の貨物保持と所在一意性を確認|
|既存ルール回帰|全190件通過（`npm test`）|
|型検査|`npm run typecheck` 通過|
|資料検査|`npm run check:docs` 通過|
|製品ビルド|`npm run build` 通過|
|ブラウザ・実機・試遊|未実施。画面の配送表示、実機、通常戦の試遊は別段階の残件|

物理側は予約先の砲台の設備準備状態と受渡し床の空きを確認し、ケースごとの古い配送予約を解放してから候補を決定する。配送先は予約済みの対象、現在の担当砲台、由来補給口に対応する砲台、残りの自陣砲台の順で安定選択する。再予約・予約解放の共通境界は従来どおり `reserve_delivery` を使い、共通側の時計・乱数・履歴は物理状態へコピーしない。このDraft PRを共通スケジュールの自動生成、R3全体、通常戦完成とは扱わない。

## Draft PR：R2b実広場と跨城侵入（2026-09-15）

対象はR2bの実広場境界を物理固定更新へ追加した変更。城内の正面出口から広場へ出る座標切替、広場の人物半径付き矩形移動、広場警備の生存世代を使った敵城入場、広場内の人物突進接触、E29・E30の限定侵入を確認した。城内の壁・門・核判定、共通側の損傷・復活・勝敗時計は既存の責任分担を維持している。通常戦の終局、観戦・結果・ランキング、実機・試遊の完成検査ではない。

|検査|結果|
|---|---|
|R2b実広場・跨城侵入|4件通過。正面出口→広場、広場警備による入場停止と討伐後の入場、広場内の人物接触、敵侵入班の跨城を確認|
|既存ルール回帰|全188件通過（`npm test`）|
|型検査|`npm run typecheck` 通過|
|資料検査|`npm run check:docs` 通過（57/57）|
|製品ビルド|`npm run build` 通過|
|ブラウザ・実機・試遊|未実施。画面の広場表示・観戦・結果画面は次段階の残件|

広場は`WorldLayout.plaza`を固定座標へ変換し、人物半径280の境界内だけを歩ける矩形として扱う。城内との出入りは正面入口でのみ行い、敵城へ入るときは`preparePlazaEntry`で対象城の広場警備を現行世代ごと確認する。許可は同じ物理更新へ記録するが、共通時計を二重に進めない。今回のDraft PRをR2全体、通常戦完成、観戦・結果・ランキング接続とは扱わない。

## PR #18：配送床の予約枠と運搬AI（2026-09-14）

対象はR3の配送予約を砲台単位から受渡し床の枠単位へ広げ、運搬AIが出発前に共通予約を取得する変更。authored floor の枠範囲、同じ砲台・枠の二重予約拒否、物理運搬AIの `reserved-carried`、受渡し後の解放、破棄時の返却、物理ケースの所在一意性を確認した。共通スケジュールの自動生成、配送先の再選択、実広場・跨城侵入、通常戦の終局、観戦・結果・ランキング、実機・試遊の完成検査ではない。

|検査|結果|
|---|---|
|R3配送床予約|4項目通過。共通の枠保持、同一枠の重複拒否、陣営別の枠分離、運搬AIの出発前予約と受渡し・破棄時の解放を確認|
|既存ルール回帰|全184件通過（PR #17までの181件＋PR #18の3件）|
|型検査|`npm run typecheck` 通過|
|資料検査|`npm run check:docs` 通過（PR作成前に再実行）|
|製品ビルド|`npm run build` 通過（PR作成前に再実行）|

配送予約は対象砲台・枠を共通状態へ保存するが、距離・見通し・物理枠の占有を共通側へ移さない。敵側AIの出発前予約と、プレイヤー側P1の手動導線（P2/P3は出発前予約をしない）を分け、枠競合で手動操作を隠さない回帰を確認対象に含める。予約解放は受渡し前の物理 handoff、`drop_object`、死亡時の荷下ろしに限定し、PR #18をR3全体、R2全体、通常戦完成とは扱わない。

## PR #17：共通補給生成と配送予約の境界（2026-09-14）

対象はR3の補給接続のうち、物理スケジュールがケースを作る前の共通 `spawn_supply` 検証と、運搬手が受渡し枠へ置く前の `reserve_delivery` 検証を追加した変更。補給口の authored port・部屋・床セル・重量・由来グループ・ID重複、運搬手の世代・所有・陣営・自陣砲台・予約ID、物理ケースの所在一意性を確認した。共通スケジュールの自動生成、配送AI、実広場・跨城侵入、通常戦の終局、観戦・結果・ランキング、実機・試遊の完成検査ではない。

|検査|結果|
|---|---|
|R3共通補給境界|3件通過。共通補給生成、重複ID拒否、運搬手の砲台配送予約と対象砲台の保持を確認|
|既存ルール回帰|全181件通過（PR #16までの178件＋PR #17の3件）|
|型検査|`npm run typecheck` 通過|
|資料検査|`npm run check:docs` 通過（57/57）|
|製品ビルド|`npm run build` 通過|

`spawn_supply` の拒否では物理側のバッグ順・グループ番号・次回補給時刻を消費せず、`reserve_delivery` の受入れ後は受渡し枠へ置く時点で予約を解放する。共通遷移用の時計・乱数・履歴は物理状態へコピーせず、PR #17をR3全体、R2全体、通常戦完成とは扱わない。

## PR #16：8種ケースの物理効果を砲撃・補給・歩行へ接続（2026-09-14）

対象はR3のケース効果のうち、物理飛行へ分割弾、補給妨害、通路妨害を接続した変更。共通カタログと同じ8種のケース定義、分割子弾の所在、補給停止／免疫時計、入口ゾーンの歩行速度、既存の迎撃・損傷・ケース一意性との組み合わせを検査した。共通側からの補給自動生成、配送AI、実広場・跨城侵入、通常戦の終局、観戦・結果・ランキング、実機・試遊の完成検査ではない。

|検査|結果|
|---|---|
|R3物理ケース効果|3件通過。分割弾の中点分割・子弾の非再分割、補給妨害の360更新停止と240更新免疫、通路妨害の半径1床・0.65速度と置換を確認|
|既存ルール回帰|全178件通過（`npm test`）|
|型検査|`npm run typecheck` 通過|
|資料検査|`npm run check:docs` 通過|
|製品ビルド|`npm run build` 通過|

分割は迎撃候補の後に一度だけ決定し、飛行物体上限96を超える場合は親を消費せず記録する。補給妨害は砲台・修理・設備停止時計を変更せず、通路妨害は対象城の現在地点にいる敵味方の歩行だけへ適用する。PR #16をR3全体、R2全体、通常戦完成とは扱わない。

## PR #15：補給ケース投影と共通待ち列投入境界（2026-09-14）

対象はR3の補給接続のうち、物理ケースを共通オブジェクトへ投影し、受渡し後の待ち列投入を共通 `enqueue_object` で再検証する変更。補給生成・配送AI・8種ケースの特殊効果・実広場・通常戦の終局・画面・実機・ランキングの完成検査ではない。

|検査|結果|
|---|---|
|R3共通補給境界|3件通過。4種ケースの共通 `weaponId`・由来投影、実ケースの受渡しから共通待ち列遷移、拒否時の受渡し復元までを確認|
|R3外装修理・設備修理|既存7件通過|
|既存ルール回帰|全175件通過（`npm test`）|
|型検査|`npm run typecheck` 通過|
|資料検査|`npm run check:docs` 通過|
|製品ビルド|`npm run build` 通過|

共通待ち列遷移用の時計・乱数・履歴は物理状態へコピーせず、物理側の一更新だけを進める。投影を正規化してから所有者・世代・陣営・容量・連続番号を検査し、拒否時は受渡し枠、貨物枠、ケース所在を復元する。8種特殊効果の物理反映、共通側からの補給自動生成、実広場・通常戦・実機・ランキングは未実施のまま残す。

## PR #14：設備修理の予約と自動復旧境界（2026-09-14）

対象はR3の設備修理を物理操作確認版へ接続した変更。砲台・補給口の自陣対象選択、ケース1個の予約、120更新の完了、外装修理予算との分離、移動中断、ケース所在の一意性、自動復旧期限と同じ更新の手動優先を確認した。8種ケースの特殊効果、補給生成・配送の共通ルール接続、実広場・跨城侵入、画面の通常戦・実機・ランキングの完成検査ではない。

|検査|結果|
|---|---|
|R3設備修理|3件通過。砲台復旧、補給口復旧、中断時のケース返却、手動／自動復旧の同更新で二重復旧なしを確認|
|R3外装修理|4件通過（既存）|
|既存ルール回帰|全件通過（`npm test`）|
|型検査|通過（`npm run typecheck`）|
|資料検査|通過（`npm run check:docs`）|
|ビルド|通過（`npm run build`）|

設備修理は完了時だけケースを消費し、外装修理予算を変更しない。設備の自動復旧・補給妨害時計・人物復活を混同せず、PR #14をR3全体または通常戦完成とは扱わない。

## PR #13：外装修理の予約と物理反映（2026-09-14）

対象はR3の外装修理を物理操作確認版へ接続した変更。修理室でのケース予約、対象部位の開始時固定、90更新の作業、完了時の回復量と累計予算、移動による中断、破壊済み・全快部位の拒否、ケース所在の一意性を確認した。設備修理、8種ケースの特殊効果、補給生成・配送の共通ルール接続、実広場・跨城侵入、画面の通常戦・実機・ランキングの完成検査ではない。

|検査|結果|
|---|---|
|R3外装修理|4件通過。`repair` 予約と `reserved-carried`、90更新後の最大12回復・ケース消費、移動・死亡中断時のケース・予算保持、開始時の対象固定と二重予約拒否を確認|
|既存ルール回帰|全件通過（`npm test`）|
|型検査|通過（`npm run typecheck`）|
|資料検査|通過（`npm run check:docs`）|
|ビルド|通過（`npm run build`）|

修理のケースは完了時だけ消費し、被弾・死亡・世代変更・修理室離脱・移動では消費しない。車両ごとの回復予算96と同部位予約を物理状態に保持し、同一ケースを砲撃・配送・別修理へ同時に使えないことを共通所在検査で確認した。PR #13をR3全体、通常戦完成、設備修理完了とは扱わない。

## PR #12：共通敵判断を物理役割へ接続（2026-09-14）

対象はPR #11の内部兵専用判断を、物理側の射手・射手護衛・弾薬運搬・内部兵の全30人へ広げた変更。共通 `chooseEnemyIntent` と固定座標観測の接続、防衛・退避の固定移動、既存の砲台・現物受渡し検証の保持を確認した。修理・補給効果、実広場、跨城侵入、画面の突進操作、実機・ランキングの完成検査ではない。

|検査|結果|
|---|---|
|R2b共通敵判断境界|3件通過。30人全員の共通意図、射手護衛の防衛、弾薬運搬の退避を確認|
|既存ルール回帰|全165件通過（PR #11までの162件＋PR #12の3件）|
|型検査|`npm run typecheck` 通過|
|資料検査|`npm run check:docs` 通過。計画全体の未実施検査は `not_run` のまま|
|製品ビルド|`npm run build` 通過|
|ブラウザ検証|ローカルはPlaywrightのChromium/WebKit実行ファイル未取得のため18件を開始前に失敗。画面入口は変更せず、GitHub Actionsで再確認する。iPhone実機、通常戦の試遊、ランキング接続・公開は未実施|

物理側は固定座標・同室の脅威・砲台・現物ケースを共通 `EnemyObservation` へ投影し、意図だけを共有する。防衛・退避の実行は物理側の壁・門・床見通し・人物半径・再使用待ちを通り、公開P1の突進または橋渡し証跡がある更新ではNPC移動を抑える。PR #12を通常戦完成・R2全体完了とは扱わない。

## PR #11：内部兵の移動目標と局所防衛（2026-09-14）

対象はPR #10の設備接触・停止復旧に、敵内部兵14人の物理目標、局所防衛突進、低体力退避を追加した変更。共通ルール側の敵判断、実広場、跨城侵入、修理・補給効果、画面の突進操作、実機・ランキングの完成検査ではない。

|検査|結果|
|---|---|
|R2b内部兵境界|3件通過。E25/E29の広場警備・限定侵入目標と固定経路移動、E17の局所防衛突進によるP1損傷、低体力時の退避を確認|
|既存ルール回帰|全162件通過（PR #10までの159件＋PR #11の3件）|
|型検査|`npm run typecheck` 通過|
|資料検査|`npm run check:docs` 通過。計画全体の未実施検査は `not_run` のまま|
|製品ビルド|`npm run build` 通過|
|ブラウザ検証|GitHub Actionsの `npm run test:browser` 通過（Chromium/WebKitモバイル、18件）。iPhone実機、通常戦の試遊、ランキング接続・公開は未実施|

内部兵の判断は同じ城・室の現在情報だけから36更新ごとに更新し、固定座標の経路・壁・門・設備判定を経由する。経路探索は1更新に最大2体ずつに分散し、探索待ちでも衝突判定付きの1歩だけを進める。公開P1の突進または明示証跡がある更新はNPCの移動を抑え、対象を動かして証跡を無効化しない。PR #11を通常戦完成・R2全体完了とは扱わない。

## PR #10：R2b設備接触の耐久・停止復旧（2026-09-14）

対象はPR #9の突進実行器に、砲台・補給口への最初の設備接触、耐久減算、停止、復旧を追加した変更。内部兵AI、実広場、画面の突進操作、補給効果・敵判断、実機・ランキングの完成検査ではない。

|検査|結果|
|---|---|
|R2b設備境界|4件通過。敵砲台への10損傷と手前停止、耐久60→0の6接触、停止中の追加損傷拒否、敵補給口への損傷、480更新期限後の耐久復旧と生成再開を確認|
|既存ルール回帰|全159件通過（PR #9までの155件＋PR #10の4件）|
|型検査|`npm run typecheck` 通過|
|資料検査|`npm run check:docs` 通過。計画全体の未実施検査は `not_run` のまま|
|製品ビルド|`npm run build` 通過|
|ブラウザ・実機・試遊|画面入口を変更しないため未実施。iPhone実機、通常戦の試遊、ランキング接続・公開は未実施|

設備停止は既存の補給妨害・外部停止と別の時計として保持し、停止中の待ち列と次回補給時刻を変更しない。停止期限の次更新で耐久を60へ戻し、`equipment_restored` を一度だけ記録する。設備接触は物理状態で完結し、人物損傷・核終局のR2b橋渡し契約は変更していない。PR #10を通常戦完成・R2全体完了とは扱わない。

## PR #9：R2b突進実行器（2026-09-14）

対象はPR #8の人物接触境界へ、固定座標の突進移動、再使用待ち、壁・閉門停止、人物／核接触証跡の自動生成を追加した変更。設備への突進損傷、実広場、内部兵AI、画面の突進操作、実機・ランキングの完成検査ではない。

|検査|結果|
|---|---|
|R2b突進境界|4件通過。1.2床を12更新で移動、54更新待ち、人物接触の自動橋渡し、閉門前停止を確認|
|R2b既存境界|既存の核・広場・人物接触8件を再実行して通過|
|既存ルール回帰|全155件通過（PR #8の151件＋PR #9の4件）|
|型検査|`npm run typecheck` 通過|
|資料検査|`npm run check:docs` 通過（57/57）。計画全体の未実施検査は `not_run` のまま|
|製品ビルド|`npm run build` 通過|
|ブラウザ・実機・試遊|画面入口を変更しないため未実施。iPhone実機、通常戦の試遊、ランキング接続・公開は未実施|

突進の接触証跡は物理側で一回だけ生成し、損傷・無敵・押し戻し・終局は共通側へ渡す。PR #9を通常戦完成・R2全体完了とは扱わない。

## PR #8：R2b人物接触の被弾境界（2026-09-13）

対象はPR #7の物理・共通一更新接続に、人物同士のダッシュ接触、損傷無敵、選択中ケース落下、押し戻しを追加した変更。証跡の検証と共通 `stepWorld` の一回消費を確認する。12更新の突進実行器、設備接触、実広場座標、補給効果・敵判断、5秒観戦画面、実機・ランキングの完成検査ではない。

|検査|結果|
|---|---|
|R2b人物接触境界|8件通過。既存の核・広場境界6件に加え、人物への1損傷・0.6床押し戻し、36更新無敵、選択中ケース一個の落下と再損傷拒否を確認|
|一更新の時計|人物接触も物理 `stepBattle` から共通 `stepWorld` を一度だけ呼び、更新番号が1回だけ進むことを確認|
|既存ルール回帰|全151件通過（PR #7の149件＋PR #8の2件）|
|型検査|`npm run typecheck` 通過|
|資料検査|`npm run check:docs` 通過。計画全体の未実施検査は `not_run` のまま|
|製品ビルド|`npm run build` 通過|
|ブラウザ・実機・試遊|物理接触証跡のテストのみ。突進操作器、実広場、iPhone実機、通常戦の試遊、ランキング接続・公開は未実施|

損傷と無敵時計、死亡・復活・荷物の所在は共通側で確定し、物理側は固定座標とケース表示へ反映する。PR #8を通常戦完成・R2全体完了とは扱わない。

## PR #7：R2b接続基盤の一更新（2026-09-13）

対象はPR #6の `r2b-bridge` を物理操作確認の固定更新へ接続した変更。物理 `stepBattle` 内で検証済みの核接触／広場入場を `stepWorld` へ一度だけ渡し、共通状態とイベントを取り込む。時計、乱数、イベント履歴を二重に進めないことを確認した。通常戦全体、人物同士の被弾・突進、実広場座標、補給効果・敵判断、5秒観戦画面、実機・ランキングの完成検査ではない。

|検査|結果|
|---|---|
|R2b接続境界|6件通過。既存の証拠検証3件に加え、物理更新からの核接触終局、広場入場、公開入力の試合束縛を確認|
|一更新の時計|物理側の `stepBattle` から共通 `stepWorld` を一度だけ呼び、核接触・広場入場とも更新番号が1回だけ進むことを確認|
|既存ルール回帰|全149件通過（PR #6の146件＋PR #7の3件）|
|型検査|`npm run typecheck` 通過|
|資料検査|`npm run check:docs` 通過。計画全体の未実施検査は `not_run` のまま|
|製品ビルド|`npm run build` 通過|
|ブラウザ・実機・試遊|画面入口を変更しないため未実施。iPhone実機、通常戦の試遊、ランキング接続・公開は未実施|

橋渡し用 `stepWorld` の時計・乱数・`lastStep`・イベント履歴を物理状態へコピーせず、共通側の終局イベントを一度だけ取り込む。PR #7を通常戦完成・R2全体完了とは扱わない。

## PR #6：R2b初回の接続境界（2026-09-13）

対象はPR #5の物理操作確認をmainへ含めた状態に、物理結果を共通ルールへ渡す `r2b-bridge` と固定経路の投影を追加した変更。R2b全体の一戦、実広場座標、人物同士の被弾、補給効果・敵判断、5秒観戦画面の完成検査ではない。

|検査|結果|
|---|---|
|接続境界|3件通過。現在の試合・更新・世代・固定位置、dashの最初の核接触、7門経路、床見通しを検証し、不正な壁接触・通常接触・古いスナップショットを拒否|
|広場突破|同3件のうち1件で確認。護衛世代の欠落・生存護衛を拒否し、全護衛撃破後だけ世代付き `entry` 入力を準備。準備中は時計と入力元を変更しない|
|既存ルール回帰|全146件通過（既存143件＋PR #6の3件）|
|型検査|TypeScript通過|
|資料検査|`npm run check:docs` 通過（57/57）。計画全体の未実施検査は `not_run` のまま|
|製品ビルド|`npm run build` 通過（TypeScript・Vite）|
|ブラウザ・実機・試遊|`npm run test:browser` はChromium/WebKitの実行ファイル未取得で18件実行前に失敗。接続モジュールは画面入口を変更しない。iPhone実機、ゲーム全体の試遊、ランキング接続は未実施|

橋渡しは時計を進めず、呼出側が一回だけ `stepWorld` へ返却入力を渡す契約とした。同じ更新で物理時計と共通時計を二重に進めた検査ではない。PR #6を通常戦完成・R2全体完了とは扱わない。

## PR #5：PR #4との衝突解消（2026-09-13）

対象はmain `28fe6a6` とPR #5 `53e4555` の統合差分。PR #4の8武器ルール入口と検査を保持し、PR #5の物理操作確認用処理を別ファイルへ移した。画面とテストの参照先を更新し、両実装の状態・命令・座標単位を混用しない。

|検査|統合後の結果|
|---|---|
|資料整合|57件通過。計画全体135件は `not_run` を維持|
|ルール・通し検査|両PRの既存141件が通過。新規公開入口2件は初回の検査用セットアップを修正し、2件とも再実行して通過。合計143件の各検査を確認|
|型検査・ビルド|新規回帰検査の修正後にTypeScript・Viteとも通過|
|Chromium画面回帰|既存9件を全件再実行して通過。名前・移動・停止・実運搬から発射・5画面サイズを保持|
|独立レビュー|Sol Highが統合差分を確認し完了。PR #4の中核処理とPR #5の移動元が各元版と完全一致、参照更新と両検査の保持を確認。今回差分のP1/P2指摘なし|
|GitHub Actions|更新後のPR #5で全143件の一括実行とChromium/WebKit計18件を確認する。最新結果は下記リンクを参照|

下記のPR #4とPR #5の数値は過去の単独検査の記録であり、統合後の合格へ読み替えない。最新の外部検査は [PR #5のQuality結果](https://github.com/chameleonjp-lab/semekome/pull/5/checks) を参照する。今回もiPhone実機、ゲーム全体の試遊、公開・ランキング接続は未実施。

## R2a（PR #5単独版の記録、2026-09-13）

対象は運搬・砲撃の操作確認版。R2bの広場戦・侵入・核攻撃まで通した通常戦、結果・ランキング、iPhone実機の合格ではない。

|検査|実施範囲|結果|
|---|---|---|
|資料整合|現行JSON、配置・人数・参照、補給上限の集計単位|57件通過。82要件・135検査計画を維持|
|時計と名前|前後空白、1〜20コードポイント、30/60/120Hz、停止中の時間破棄|3件通過|
|ブラウザ操作|名前・二重開始・カウントダウン中止、移動、停止・明示再開、5画面サイズ、通常操作の運搬→発射、名前のDOM安全性|ChromiumでR1の4件を含む全9件が一括通過|
|複数pointerの入力境界|移動の指固定、別指の無視、pointercancel、blur、入力破棄|隔離した入力アダプター検査がChromiumで通過。実機の二本指操作とは別|
|ルール・長時間の通し検査|所有と物流、補給配分、4砲台、迎撃、7部位・門、長時間継続|時計3件・R1の22件・R2aルール13件・幾何8件・通し2件、合計48件通過|
|日本語の画面目視|390×664と844×390|確認済み。検査環境へ日本語フォントを注入。製品依存に追加しない|
|型検査・製品ビルド|TypeScript、Vite静的ビルド|最終ソースで再実行し通過|
|独立レビュー|Sol Highの画面・入力・時計・エンジンと最終差分の再確認|完了。R2a通常操作に残るP1/P2なし。独立実行でも48件通過。R2bの所在分離・履歴圧縮は進捗の残件へ記録|
|GitHub Actions|資料・全ルール・ビルド・Chromium/WebKit|初回の画面3件失敗を修正し、UI修正版0ef77ee3で全段階通過（画面18件）。受渡し修正を含む最終版は[PR #5の最新Quality結果](https://github.com/chameleonjp-lab/semekome/pull/5/checks)を参照|

ブラウザ用のスキルに従って確認を開始したが、agent-browserは起動時に失敗したため、Playwrightで代替した。ローカルは既存Chromium 143を指定し、WebKitはCI側の結果と分ける。自動テスト中の編集によるページ再読み込みを避けるため、テスト専用サーバーではHMR（変更の自動反映）を無効化した。移動量の比較は時計を明示停止し、位置読取と指を離す操作の間に実時間の1更新が混ざらないようにした。

固定seed 20260913の25,000更新（戦闘時間416.7秒）の通し検査では、P1が通常入力で4回運び、自陣80発・敵428発・迎撃136回。最終発射は自陣24,960更新・敵24,894更新で、終盤20,000更新以降も両側が継続した。由来・所持枠・固定受渡し枠・待ち列・飛翔体の一意な所在を毎更新確認した。両側への実ダメージを確認し、外装・門の7段階は専用ルール検査と分担した。自陣7門開放・敵0門というこの固定操作の結果はバランス合格ではない。24往復や両城全壊をこの操作列の成立条件にはしない。1000更新の計測は約1.4秒、25,000更新の監査付き検査は約53秒であり、iPhone実機FPSを表す値ではない。

CIで開始の二重タップが切替直後の中止ボタンへ当たる不具合を検出したため、切替直後350msの中止入力を無視し、初期表示・操作遮断を初回描画待ちにせず適用した。名前・中止検査の時計を明示停止し、停止中40秒の検査は不要な全描画を再生せず時間経過だけ進める形へ変更した。停止中の更新不変と、再開時に過去時間を追いつかせない条件は維持した。最終レビューでは、受渡し2枠の実位置、装填時の最新経路・部位、重量制限を含む表示候補と実際に拾う弾の一致も回帰検査した。

今回の検査で扱うのはR2aの部分条件である。`ACCEPTANCE_TESTS.json` 全135件を実行済みに変更しない。実機性能、試遊・ゲームバランス、名称・素材の権利確認、公開、外部ランキング接続は未実施のまま。

## R2a 共通ルール（PR #4の記録、2026-09-13）

対象は共通砲撃ルール、外部操作の検証、敵4役割の判断基盤。通常戦画面は変更していない。下記のR1記録は前回の履歴として保持する。

|検査|実施範囲|今回の状態|
|---|---|---|
|資料整合|現行資料57項目。射手必須の設定修正を含む|57件通過|
|ルール|R1の22件＋砲撃81件＋操作・敵判断12件|計115件通過|
|TypeScript・ビルド|新規ルールと既存画面の型・静的ビルド|両方通過|
|ブラウザ回帰|既存の配置確認画面、Chromium・WebKit|今回のローカル検査は未実施。作業ブランチのGitHub Actionsで確認|
|独立レビュー|別担当による今回差分の読取|未実施。実装担当の自己点検と自動検査のみ|
|実機・試遊・負荷・外部接続|iPhone、連続実戦、補給・歩行との効果接続、ランキング|未実施|

砲撃検査は8×8の64組合せ、区間接触・時刻順・ID同値順、発射更新の静止、直通360／迂回468更新、4砲台の48更新共有枠、射手死亡・離席・保護・世代・設備停止、待ち列保持、同更新の着弾目標、損傷の非繰越、全外装破壊後も核に届かないこと、分裂の3子・総損傷12・再分裂禁止・96上限、妨害と免疫の期限・減速係数を含む。

操作・敵判断検査は損傷／命中／移動通知の注入拒否、NPCへのなりすまし拒否、試合と世代、距離・城・部屋・重量・所持枠、修理予約の装填禁止、対象の勝手な変更禁止、操作権排他、停止／明示再開、観戦中の入力拒否と復活、未発見の部屋情報に依存しない判断、現物の二重取得防止、判断周期と死亡・復活時計の分離を含む。30／60／120描画相当の更新回数のまとめ方を変えて同じ最終状態になることを検査した。実際の描画性能測定ではない。

テストは人物を砲台や床に明示配置してから動かす。人物がそこまで歩いたこと、敵の移動目標・自衛・退避が実行されたこと、配送予約・補給生成・設備破壊が完成したことは検証していない。`ACCEPTANCE_TESTS.json` の全135件を実行済みとはせず、計画の `not_run` を維持する。

## R1（PR #3取り込み済み）の記録

対象は第5版に基づく基礎モデルと配置確認画面。通常対戦・実機性能・外部ランキング接続の合格を意味しない。

### R1の実施状況

|検査|実施範囲|結果|
|---|---|---|
|資料整合|現行JSON、要件・検査ID、名簿、配置、参照、採用記録|57件通過。82要件・135件の検査計画を確認|
|ルール自動検査|両城の独立状態、7部位・7門・核、床の接続、個別復活、世代・試合ID、所在、停止と再開|修正後22件通過|
|TypeScript・製品ビルド|型検査とViteの静的ビルド|修正後に通過|
|ブラウザ自動検査|ホーム・ルール・配置の往復、鏡面配置と敵30人、設備・復活床、カメラ、ドラッグ中断、画面の収まり|Chromiumで4件通過|
|日本語表示の目視確認|390×664の縦画面、844×390の横画面|確認済み。日本語フォントを検査環境へ注入して表示|
|独立レビュー|Sol Highによる画面・基礎モデル・資料の読取レビュー|再レビュー完了。P1/P2の残存なし|
|GitHub Actions|資料、ルール、ビルド、Chromium・WebKit|結果は[作業ブランチのQuality実行履歴](https://github.com/chameleonjp-lab/semekome/actions/workflows/quality.yml?query=branch%3Afeat%2Fr1-foundation-v5)を参照|

自動画面検査では360×640、390×664、402×700、430×932、844×390を使用した。主要ボタン48px以上、画面外へのはみ出し、部屋選択・説明の表示、ルールダイアログの閉鎖とフォーカス復帰を確認した。配置画面は戦闘時計を進めない。

ローカル環境ではPlaywright標準ブラウザの取得に失敗したため、Chromium 143を指定して検査した。ローカルのWebKit検査は未実施。CIのブラウザ検査はローカル結果と分けて扱う。

### R1のレビューで確認した修正点

コア命中を確定する内部イベントの境界、試合ID・生存世代を欠く入力の拒否、飛翔体と運搬物の参照、予約の解放、両城の待ち列の独立性を修正した。発射後の待ち列を詰め、同じ場所へ重複して入らないことも検査した。図の後方通路と実床を整合させ、両城の7門をそれぞれ一枚だけ閉じた14通りで核への道が遮断されることを検査した。

資料に残る旧設備数と未実装の表記、自陣人物の重なり、設備と復活床の表示、役割ごとの図形識別を修正した。

自陣3人の復活床を配置JSONに統一し、コードによる追加生成を廃止した。人物と初期室・復活室・担当床の対応も設定時に検査し、核室への不正な床移設や床の欠落を拒否する。門の回帰検査は各人物の初期室と復活床が属する室を対象とした。

### R1提出時点の未実施・次段階（履歴）

- R2の実移動・突進の連続当たり判定、砲撃、運搬・装填、敵行動、広場横断、名前登録・カウントダウンを通した一戦。
- 1床単位＝1000整数の移動用座標。R1の位置はセル座標であり、床の接続検査は人物半径や連続衝突を検証していない。
- R3以降の配分・修理・妨害・命令、練習、結果表示、得点、共有、上位10位ランキング。
- iPhone実機のSafari、片手操作、音、発熱・電池・FPS、長時間試遊・連続再戦。
- ランキング接続とサーバー側検証。登録先・game_slugを推測した接続は行っていない。

`docs/plans/current/ACCEPTANCE_TESTS.json` の135件は計画全体の検査仕様。R1の部分検査から個々の受入検査全体の合格へ読み替えず、すべて `not_run` を維持する。採用画像の破損記録も、正常な原本を取得するまで未解決とする。
